# Проект тестирования с Allure

## Установка зависимостей

```bash
pip install selenium allure-pytest pytest


#Для запуска тестов и формирования отчета используется команда:pytest --alluredir=allure_results
#Для просмотра отчета используется команда: allure serve allure_results